import { ToImageOWPipe } from './to-image-ow.pipe';

describe('ToImageOWPipe', () => {
  it('create an instance', () => {
    const pipe = new ToImageOWPipe();
    expect(pipe).toBeTruthy();
  });
});
